name: Requirements Pipeline

on:
  workflow_call:
    inputs:
      business_idea:
        description: 'Business idea or product description'
        required: true
        type: string

jobs:
  requirements:
    runs-on: ubuntu-latest
    name: Requirements Engineering
    steps:
      - name: Requirements Engineering
        run: |
          echo "========================================"
          echo "Requirements Engineering"
          echo "========================================"
          echo "Business Idea: ${{ inputs.business_idea }}"
          echo "Model: google/gemma-4-31b-it:free"
          echo ""
          echo "Activities:"
          echo "  - Generate BRD"
          echo "  - Commit to docs/brd/BRD.md"
          echo "  - Create pull request"
          echo ""
