name: Deploy to Development

on:
  workflow_dispatch:
  push:
    branches: [develop]

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    environment: development
    name: Deploy DEV
    steps:
      - name: Deploy DEV stub
        run: |
          echo "Deploy DEV pipeline stub - to be implemented"
          echo "Stage: Development"
          echo "Activities:"
          echo "  - Build application"
          echo "  - Run unit tests"
          echo "  - Deploy to DEV environment"
          echo "  - Run smoke tests"
